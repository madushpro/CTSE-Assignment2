"""
Requirements file for AI Travel Planner Multi-Agent System

This project requires:
- Python 3.8+
- Ollama (local LLM engine) with llama3:8b model
- Python packages listed below
"""

# Web UI Framework
streamlit>=1.28.0

# Data processing and testing
pandas>=1.3.0
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional but recommended
python-dotenv>=0.19.0
requests>=2.28.0

# Note: This project does NOT use:
# - OpenAI (paid API)
# - Anthropic (paid API)
# - LangChain (uses CrewAI framework instead)

# LLM Integration would be via Ollama:
# - Ollama must be installed locally
# - Model: llama3:8b (pull with: ollama pull llama3:8b)
# - Runs on http://localhost:11434
